{
  "name": "capacitor-kiosk-example",
  "version": "0.1.0",
  "private": true,
  "description": "Minimal Capacitor 8 host app exercising @gachlab/capacitor-kiosk. Android-only (the plugin has no iOS).",
  "scripts": {
    "sync": "cap sync",
    "open:android": "cap open android"
  },
  "dependencies": {
    "@capacitor/android": "^8.0.0",
    "@capacitor/core": "^8.0.0",
    "@gachlab/capacitor-kiosk": "file:.."
  },
  "devDependencies": {
    "@capacitor/cli": "^8.0.0"
  }
}
