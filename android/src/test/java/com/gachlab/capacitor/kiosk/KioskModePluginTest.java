package com.gachlab.capacitor.kiosk;

import static org.junit.Assert.*;

import org.junit.Test;

public class KioskModePluginTest {

    @Test
    public void pluginCanBeInstantiated() {
        KioskModePlugin plugin = new KioskModePlugin();
        assertNotNull(plugin);
    }
}
